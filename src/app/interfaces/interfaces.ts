export interface actor{
    id: number,
    nombre: string,
    fechaNacimiento: string,
    foto: string
}

export interface pelicula{
    id: number,
    titulo: string,
    enCines: boolean,
    fechaEstreno: string,
    poster: string
}